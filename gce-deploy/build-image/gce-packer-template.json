{
    "variables": {
        "account_file": "",
        "project_id": "",
        "source_image": "ubuntu-1604-xenial-v20190306",
        "ssh_username": "jeffssh",
        "zone": "us-west1-a"
    },

    "builders": [
        {   
            "type": "googlecompute",
            "account_file":"{{user `account_file`}}",
            "project_id": "{{user `project_id`}}",
            "source_image": "{{user `source_image`}}",
            "ssh_username": "{{user `ssh_username`}}",
            "zone": "{{user `zone`}}"
        }
    ],

    "provisioners": [
        {
            "type": "file",
            "source": "./install_scripts/welcome.txt",
            "destination": "/home/jeff/"
        },
        {
            "type": "shell",
            "scripts": [
                "install-scripts/add-user.sh"
            ]
        }
    ]
}
